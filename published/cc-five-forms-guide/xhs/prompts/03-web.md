# Card 03 — Web 版

XHS card 03, portrait 1024×1536. Hand-drawn sketch-notes style, palette per `_style.md`.

**Hero (top 55%)**: a large hand-drawn web browser window — three tiny dots top-left, a URL bar in the middle showing `claude.ai/code` (graphite gray monospace, glyphs must render correctly), one tab labeled `Code`. Inside the page area: a few faint chat-bubble shapes suggesting a conversation. Around the browser window, draw a tiny hand-drawn cloud icon with a thin misty-blue line implying "runs in the cloud, not your machine".

**Title block**:
- Big bold ink-black: `③ Web 版`
- Subtitle graphite gray: `随手开 · 但水土不服`

**Three feature labels** (lower, each with phosphor-green dot):
- 不装、不配环境
- 跑云端 sandbox
- 长任务别赌

**Bottom row**: TWO small pill tags side-by-side:
- graphite-gray pill: `适合：临时改 / 出差应急`
- phosphor-green outlined pill: `Cowork ≠ Web Code`

**Watermark**: bottom-right "雷码工坊笔记".

Forbidden: pure black, purple gradients, orange, neon, photographic realism, fake CJK.

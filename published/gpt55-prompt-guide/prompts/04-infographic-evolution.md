Create a professional infographic following these specifications:

## Image Specifications

- **Type**: Infographic
- **Layout**: linear-progression (Timeline variant)
- **Style**: technical-schematic (Blueprint variant)
- **Aspect Ratio**: 16:9
- **Language**: zh + English mix

## Core Principles

- Follow the linear-progression layout precisely: 4 timeline nodes arranged horizontally, each marking a GPT generation
- Apply technical-schematic style: deep blue background, white grid lines, blueprint aesthetic, monospace technical labels
- Keep text minimal and use exact strings provided — DO NOT generate alternative wording or paraphrase
- All text must be sharp, legible, no glyph errors

## Text Requirements (use these strings EXACTLY)

- Main title (top, white, all caps): `PROMPT WRITING — FOUR GENERATIONS`
- Subtitle (smaller, light cyan): `每一代模型变强 = 使用者代偿减少一层`
- Four timeline node labels:
  1. `GPT-3 · 2021` — sub-label: `Few-shot stacking`
  2. `GPT-4 · 2023` — sub-label: `Role + CoT + XML`
  3. `GPT-5 · 2024` — sub-label: `ALWAYS / NEVER rules`
  4. `GPT-5.5 · 2026` — sub-label: `Outcome-first · minimal`
- Bottom-line caption (small, white): `Each generation grows smarter → users write less to compensate`

## Layout Guidelines (linear-progression)

- Horizontal timeline with 4 evenly-spaced nodes
- Each node is a stylized rectangular "spec card" containing a tiny prompt-sample preview underneath
- A connecting line runs through all four nodes with arrowheads pointing right
- The connecting line is annotated with year markers
- Below each node: a horizontal density bar that visually shrinks left → right (red wide → green narrow), expressing "prompt 越来越短"

## Style Guidelines (technical-schematic, Blueprint variant)

- Deep blueprint blue background (#0F2A44 or #1E3A5F), faint white grid pattern
- White line work, technical drafting aesthetic
- Amber accent (#F59E0B) on the final GPT-5.5 node and density bar to emphasize the destination
- All text in monospace technical stencil or clean sans
- Use measurement-line decorations (small ticks, dimension markers) to reinforce engineering feel
- No realistic figures, no decorative gradients, no glow effects

## Layout details for each node

Each timeline node should contain a small "code preview block" showing density:

- GPT-3 node: ~6 short lines of `Q:.../A:...` patterns
- GPT-4 node: 4-5 lines with visible XML angle brackets `<task>` `<instructions>`
- GPT-5 node: 4-5 lines with capital words `ALWAYS` `NEVER` `MUST`
- GPT-5.5 node: just 2 lines of clean text

Density bar widths approximately: 100% → 95% → 80% → 15%

## Strict prohibitions

- No purple gradients
- No glowing neon
- No robots, faces, mascots
- No decorative AI illustrations
- No floating geometric confetti
- No misspelled English words

## Mood

Calm, technical, blueprint-of-history. Like an engineering diagram on a developer's whiteboard.
